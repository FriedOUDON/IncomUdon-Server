This directory is mounted only into the external Management Service container.
It must contain `client.crt`, `client.key`, and `relay-ca.crt`. Do not place
the Relay server private key or the Relay authorization CSV in this directory.
